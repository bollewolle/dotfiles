# 💡 Tip: you can re-enter your name and email with `chezmoi init --data=false`.\n"
{{- $name := "Bram Goolaerts" -}}
{{- $email := "dev@bollewolle.be" -}}
{{- $username := "bramg" -}}
{{- $github_username := "bollewolle" -}}

{{- $data := . }}
{{- $_ := set $data "host" (default (dict) (get $data "host")) -}}
{{- $_ := set $data "user" (default (dict) (get $data "user")) -}}
{{- $_ := set $data "username" (default (dict) (get $data "username")) -}}
{{- $_ := set $data "github_username" (default (dict) (get $data "github_username")) -}}

[data]
    onepasswordAccount = "tbd"

[data.user]
    email = {{ $email | quote }}
    name = {{ $name | quote }}
    username = {{ $username | quote }}
    github_username = {{ $github_username | quote }}

[hooks.read-source-state.pre]
    command = ".local/share/chezmoi/.install-password-manager.sh"